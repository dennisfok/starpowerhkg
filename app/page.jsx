export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>New Project Initialized</h1>
      <p>You are now in a fresh project scaffold, independent from the old starpowerhkg setup.</p>
      <ol>
        <li>Edit <code>app/page.jsx</code> to build your new app.</li>
        <li>Run <code>npm install</code> if needed.</li>
        <li>Start development with <code>npm run dev</code>.</li>
      </ol>
    </main>
  );
}
